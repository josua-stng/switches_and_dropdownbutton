import 'package:flutter/material.dart';

class Pertemuan2DetailScrenn extends StatelessWidget {
  const Pertemuan2DetailScrenn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pertemuan 02')),
      body: Column(
        children:
       [Center(child: Text('Detail Minggu 2'),)

       ]),
    );
  }
}